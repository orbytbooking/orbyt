"use client";

/**
 * Form 2 add-on editor — writes to `industry_form2_addons` only (`booking_form_scope=form2`, `listing_kind=addon`).
 */
import Form2ExtrasNewPage from "../../extras/new/page";

export default function Form2AddOnsNewPage() {
  return <Form2ExtrasNewPage />;
}
